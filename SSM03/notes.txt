1.pom.xml中总共配置了两个服务器,jetty,tomcat
jetty：
	<%
	request.setAttribute("rootpath", request.getContextPath());
	String basePath = request.getServerName() + ":"
			+ request.getServerPort();
	%>
	这个东西用不了,直接用"/"就可以
tomcat:
	可以用

-----------------------------------------------------------------
2.